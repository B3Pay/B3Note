{
  "name": "b3-note",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dfx:start": "sh dfx-start.sh",
    "deploy": "dfx deploy",
    "generate": "dfx generate",
    "dev": "source ./frontend/.env && cd frontend && yarn dev",
    "build": "cd frontend && yarn build",
    "start": "cd frontend && yarn start",
    "lint": "cd frontend && yarn lint",
    "wasm-pack": "cd ic-vetkd-utils && wasm-pack build --release --target web && wasm-pack pack"
  }
}
